#pragma once

// Copy this file to `include/mysecret.h` and fill in your private values.
// IMPORTANT: `include/mysecret.h` is ignored by git (see `.gitignore`).

// Example secrets used by the firmware
static const String PhantomPass = "REPLACE_ME";
static const String MainSolWallet = "REPLACE_ME";
