#pragma once

#include <Arduino.h>

// NOTE: Use `static` so this header can be included by multiple translation units
// without causing multiple-definition linker errors.
static const String PhantomPass = "BIGBREAD";
static const String MainSolWallet = "BIGGERBREAD";